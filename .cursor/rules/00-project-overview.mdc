---
description: Repo map and non-negotiables for Paradise Spas dealer template
alwaysApply: true
---

# Project overview

- Open the workspace at the **repo root**. Read `AGENTS.md` first.
- **Today:** `apps/site/` on Cloudflare Pages + Functions. See `docs/agent/STACK.md`.
- Ops truth: `docs/ops/`. Agent map: `docs/agent/`. Tooling: `docs/tooling/`.
- SOPs: `apps/dashboard/`. Portable kit: `packages/agency-starter/`.
- Do not scaffold Next.js / Vercel unless the user explicitly scopes that work.
- Playbook (`docs/PLAYBOOK.md`) — only when the task is conversion strategy.
